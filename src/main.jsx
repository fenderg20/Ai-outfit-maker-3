import React from "react";
import ReactDOM from "react-dom/client";
import OutfitMaker from "./OutfitMaker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OutfitMaker />
  </React.StrictMode>
);
